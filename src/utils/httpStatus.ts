/**
 * Enum representing common HTTP status codes.
 *
 * @enum {number}
 */
export enum HttpStatus {
    Ok = 200,
    Accepted = 202,
    BadRequest = 400,
    NotFound = 404,
    InternalServerError = 500,
}
